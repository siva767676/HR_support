[pytest]
# add the project root to sys.path so `import app.*` works without installing
pythonpath = .
# let async test functions run without an explicit @pytest.mark.asyncio decorator
asyncio_mode = auto
testpaths = tests
